{
  "name": "superhero-builder-game",
  "version": "0.1.0",
  "description": "A standalone kids' superhero-builder app (age 6-9): describe a character, get an AI-generated image, pick a type, set gender/powers, build tools and a habitat, then sell/trade/print/3D-print/collect cards for it in a marketplace.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.5.3"
  }
}
